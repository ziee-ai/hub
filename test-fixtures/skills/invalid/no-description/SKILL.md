---
name: no-description-skill
when_to_use: This skill omits the required `description` field on purpose.
---

# No-description skill

The frontmatter above is missing the required `description` field, which the
model needs in order to know when to invoke the skill. The validator must
reject this.
